-- numeric functions
-- addition
SELECT A+B FROM TABLE;
-- SUBSTRACTION
SELECT A-B FROM TABLE;
--MUL
SELECT A*B FROM TABLE;
-- DIV
SELECT A/B FROM TABLE;
-- ABS ABS
SELECT ABS(A) FROM TABLE;
--CEIL
CEIL(A)
-- EXP
EXP(A)
-- FLOOR
FLOOR(A)
--LOG
LOG(A,10)-- BASE
-- MOD,POWER,SIN,TAN,SQRT,ROUND

-- CHARCTER FUNCTION
-- lower,upper
SELECT LOWER('GEEKSFORGEEKS') FROM DUAL;
-- INITCAP : This function converts alpha character values to uppercase for the first letter of each word and all others in lowercase.
-- concat 
CONCAT('String1', 'String2')
-- length
LENGTH(STRING)
--SUBSTRING
SUBSTR('String',start-index,length_of_extracted_string)
-- TRIM 
SELECT TRIM('        geeksforgeeks   ') FROM DUAL; 
-- REPLACE
 REPLACE(Text, search_string, replacement_string)

-- DATE AND TIME FROM DOCUMENT
